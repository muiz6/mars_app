import 'package:flutter/material.dart';

abstract class Dimensions {
  static const Radius RADIUS_S = Radius.circular(10);

  static const double MARGIN_M = 8;

  static const double PADDING_M = 10;
}